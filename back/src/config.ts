import { abi } from './abi';
const config = {
  // jsonRpcUrl: 'http://127.0.0.1:8545',
  jsonRpcUrl: 'https://sepolia.infura.io/v3/....',
  contractAddress: '....',
  contractAbi: abi,
  mnemonic:''
};

export default config;
